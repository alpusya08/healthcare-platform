-- Initial schema placeholder.
-- Real tables (users, appointments, ai_analysis_sessions, audit_log, ...)
-- will be added in subsequent migrations on roadmap Day 2+.
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
